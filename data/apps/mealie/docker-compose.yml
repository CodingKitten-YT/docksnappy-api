name: mealie
services:
  mealie:
    container_name: mealie
    image: hkotel/mealie:v2.4.2
    restart: unless-stopped
    volumes:
    - mealie-data:/app/data
    ports:
    - 9925:9000
    hostname: mealie
minVersion: 0.14.0
