name: beszel
services:
  beszel:
    container_name: beszel
    image: henrygd/beszel:0.9.1
    restart: unless-stopped
    volumes:
    - beszel-data:/beszel_data
    ports:
    - 8090:8090
    hostname: beszel
minVersion: 0.14.0
