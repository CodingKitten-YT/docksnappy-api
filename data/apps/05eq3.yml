certbot:
  image: certbot/certbot:latest
  volumes:
    - './letsencrypt:/workspace' # Let's Encrypt storage
    - './directus-le:/etc/letsencrypt' # Let's Encrypt certificates and keys
  ports:
    - "80:80"
    - "443:443"
  depends_on:
    - directus
  networks:
    - directus-net