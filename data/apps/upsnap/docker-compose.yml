name: upsnap
services:
  upsnap:
    container_name: upsnap
    image: ghcr.io/seriousm4x/upsnap:4.5.1
    restart: unless-stopped
    network_mode: host
    volumes:
    - upsnap-data:/app/pb_data
    hostname: upsnap
minVersion: 0.14.0
