name: dbgate
services:
  dbgate:
    container_name: dbgate
    image: dbgate/dbgate:6.1.3-alpine
    restart: unless-stopped
    volumes:
    - dbgate-data:/root/.dbgate
    ports:
    - 3000:3000
    hostname: dbgate
minVersion: 0.14.0
