name: node-red
services:
  node-red:
    image: nodered/node-red:4.0.8
    network_mode: host
    ports:
    - 1880:1880/tcp
    restart: always
    volumes:
    - type: volume
      source: node-red-data
      target: /data
    container_name: node-red
    hostname: node-red
minVersion: 0.14.0
